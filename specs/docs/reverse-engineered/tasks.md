# Physical AI & Humanoid Robotics — Interactive Textbook Implementation Tasks

**Version**: 1.0 (Reverse Engineered)
**Date**: 2026-06-26

## Phase 1: Core Infrastructure (Week 1)

### Task 1.1: Project Setup

- [ ] Create monorepo root directory structure
  - [ ] `Frontend/` — Docusaurus application
  - [ ] `Backend/` — Python services
  - [ ] `Backend/Connection/` — Connection API (port 8000)
  - [ ] `Backend/Agent/` — Agent API (port 8001)
  - [ ] `Backend/Ingestion/` — Book ingestion pipeline
  - [ ] `Backend/Retrieval/` — Vector search module
- [ ] Initialize `uv` workspace in `Backend/`
  - [ ] Create `Backend/pyproject.toml` with `[tool.uv.workspace]`
  - [ ] Create `Backend/uv.lock`
- [ ] Initialize npm project in `Frontend/`
  - [ ] Create `Frontend/package.json` with Docusaurus 3.9.2 dependency
  - [ ] Run `npm install` to generate lockfile
- [ ] Create `.gitignore` at repo root
  - [ ] Ignore `node_modules/`, `.venv/`, `__pycache__/`, `.env`, `build/`, `.docusaurus/`
- [ ] Create `AGENTS.md` at repo root with architecture overview and quick start commands

### Task 1.2: Configuration System

- [ ] Create `.env` template at repo root
  - [ ] Key: `OPENROUTER_API_KEY`
  - [ ] Key: `OPENROUTER_BASE_URL` (default: `https://openrouter.ai/api/v1`)
  - [ ] Key: `OPENROUTER_MODEL` (default: `qwen-2.5-72b-instruct`)
  - [ ] Key: `COHERE_API_KEY`
  - [ ] Key: `QDRANT_API_KEY`
  - [ ] Key: `QDRANT_URL`
  - [ ] Key: `COLLECTION_NAME` (default: `book_embeddings`)
  - [ ] Key: `TOP_K` (default: `3`)
  - [ ] Key: `SESSION_MEMORY_TURNS` (default: `5`)
  - [ ] Key: `SESSION_TTL` (default: `1800`)
  - [ ] Key: `DEBUG` (default: `False`)
- [ ] Implement settings via `pydantic-settings` in `Backend/`
  - [ ] `Backend/Connection/config.py` — `Settings` class with field validation
  - [ ] `Backend/Agent/settings.py` — `Settings` class with singleton pattern
  - [ ] `Backend/Retrieval/src/retrieval/config.py` — `Settings` class
  - [ ] Auto-load `.env` from repo root (search parent directories)
- [ ] Frontend environment configuration
  - [ ] Hardcode backend URL in `CustomFields` via `docusaurus.config.ts:31`
  - [ ] Hardcode `BACKEND_URLS` array in `ChatKit.tsx:14-18`

### Task 1.3: Logging Infrastructure

- [ ] Configure Python logging in `Backend/`
  - [ ] `Backend/Agent/logging_config.py` — structured JSON logging
  - [ ] `Backend/Connection/logging_config.py`
  - [ ] `Backend/Ingestion/logging_config.py`
- [ ] Add request ID middleware for log correlation
- [ ] Add error detail logging in exception handlers
- [ ] Configure log levels via `DEBUG` env var (DEBUG/INFO/ERROR)

## Phase 2: Data Layer — Book Content (Week 2)

### Task 2.1: Book Content Structure

- [ ] Create `Frontend/docs/` directory with 4 modules
  - [ ] `Frontend/docs/module1-introduction/` — Introduction to Physical AI
    - [ ] `01-history-and-evolution.md`
    - [ ] `02-core-concepts.md`
  - [ ] `Frontend/docs/module2-perception/` — Perception & Sensing
    - [ ] `01-sensor-technologies.md`
    - [ ] `02-computer-vision.md`
  - [ ] `Frontend/docs/module3-control/` — Control & Planning
    - [ ] `01-kinematics-dynamics.md`
    - [ ] `02-motion-planning.md`
  - [ ] `Frontend/docs/module4-interaction/` — Human-Robot Interaction
    - [ ] `01-natural-language.md`
    - [ ] `02-ethical-considerations.md`
- [ ] Create supplementary pages
  - [ ] `Frontend/docs/intro.md` — Landing page / textbook overview
  - [ ] `Frontend/docs/setup-guide.md` — How to use the textbook
  - [ ] `Frontend/docs/glossary.md` — Key terminology

### Task 2.2: Docusaurus Sidebar Configuration

- [ ] Configure `Frontend/sidebars.ts` with 4 autogenerated module categories
  - [ ] Category "Module 1: Introduction to Physical AI" → `docs/module1-introduction/`
  - [ ] Category "Module 2: Perception & Sensing" → `docs/module2-perception/`
  - [ ] Category "Module 3: Control & Planning" → `docs/module3-control/`
  - [ ] Category "Module 4: Human-Robot Interaction" → `docs/module4-interaction/`
- [ ] Add link items for intro, setup-guide, glossary at top of sidebar
- [ ] Verify sidebar renders all 8 chapter files correctly

### Task 2.3: Docusaurus Theme Setup

- [ ] Swizzle Navbar component
  - [ ] Custom logo/branding for Physical AI textbook
  - [ ] Dark mode toggle
  - [ ] Search bar (local search plugin)
- [ ] Swizzle Footer component
  - [ ] Remove default Docusaurus footer
  - [ ] Add custom footer with project info, links, attribution
- [ ] Configure `docusaurus.config.ts`
  - [ ] Set `title: "Physical AI & Humanoid Robotics"`
  - [ ] Set `tagline` and `url`
  - [ ] Configure `navbar`, `footer`, `sidebar`, `toc` settings
  - [ ] Add `customFields` for backend URL
  - [ ] Add `preset-classic` with docs and blog disabled as needed
  - [ ] Configure `lastUpdated` and edit URL

## Phase 3: Data Layer — Vector Database (Week 3)

### Task 3.1: Qdrant Collection Setup

- [ ] Create `Backend/Retrieval/src/retrieval/client.py`
  - [ ] Initialize `QdrantClient` with `QDRANT_URL` and `QDRANT_API_KEY`
  - [ ] Implement `get_collection_info()` — fetch collection metadata
  - [ ] Implement `check_connection()` — health check helper
  - [ ] Implement `recreate_collection()` for dev/reset flows
  - [ ] Error handling: connection failures, auth errors, timeout
- [ ] Define collection schema
  - [ ] Collection name from `COLLECTION_NAME` env var
  - [ ] Vector size: 1024 (Cohere embed-multilingual-v3.0)
  - [ ] Distance metric: Cosine
  - [ ] Payload fields: `module`, `chapter`, `heading`, `text`, `chunk_index`, `source_file`

### Task 3.2: Cohere Embedding Integration

- [ ] Create `Backend/Retrieval/src/retrieval/embedder.py`
  - [ ] Initialize `Cohere` client with `COHERE_API_KEY`
  - [ ] Implement `embed_texts(texts: List[str]) -> List[List[float]]`
  - [ ] Model: `embed-multilingual-v3.0`
  - [ ] Implement `embed_query(query: str) -> List[float]` (single text overload)
  - [ ] Batch processing for large document sets
  - [ ] Error handling: rate limits, API failures, timeout
  - [ ] Retry logic: 3 attempts with exponential backoff

### Task 3.3: Ingestion Pipeline

- [ ] Create `Backend/Ingestion/src/ingestion/chunker.py`
  - [ ] Recursive markdown-aware chunking
  - [ ] Split by headings, paragraphs, sentences with configurable overlap
  - [ ] Preserve metadata (module, chapter, heading hierarchy, source file)
  - [ ] Configurable `chunk_size` and `chunk_overlap`
- [ ] Create `Backend/Ingestion/src/ingestion/document_processor.py`
  - [ ] Read markdown files from `Frontend/docs/`
  - [ ] Parse frontmatter (title, module, chapter metadata)
  - [ ] Extract heading hierarchy (h1, h2, h3)
  - [ ] Split into sections using `chunker.py`
  - [ ] Return list of `DocumentChunk` objects with metadata
- [ ] Create `Backend/Ingestion/src/ingestion/text_cleaner.py`
  - [ ] Strip markdown formatting for clean text storage
  - [ ] Normalize whitespace and special characters
  - [ ] Handle code blocks and inline code
- [ ] Create `Backend/Ingestion/src/ingestion/vectorizer.py`
  - [ ] Orchestrate: DocumentChunk → embed → prepare Qdrant point
  - [ ] Assign UUID per chunk for idempotent upsert
  - [ ] Filter empty/short chunks
- [ ] Create `Backend/Ingestion/src/ingestion/vector_store.py`
  - [ ] Implement `upsert_chunks(chunks: List[Dict])` — batch upsert to Qdrant
  - [ ] Implement `delete_all()` — clear collection for re-ingestion
  - [ ] Progress logging for large ingestion jobs
  - [ ] Error handling: partial failures, retry
- [ ] Create `Backend/Ingestion/src/ingestion/embedder.py`
  - [ ] Thin wrapper around Cohere embedder from Retrieval module
- [ ] Create main ingestion script/entry point
  - [ ] Orchestrate full pipeline: scan docs → process → embed → store
  - [ ] CLI flags for partial re-ingestion, collection reset

## Phase 4: Backend — Retrieval Service (Week 4)

### Task 4.1: Qdrant Search Client

- [ ] Create `Backend/Retrieval/src/retrieval/search.py`
  - [ ] Implement `search_similar(query: str, top_k: int, filters: Dict | None) -> List[Dict]`
    - [ ] Embed query via `embedder.embed_query(query)`
    - [ ] Call `qdrant_client.search()` with payload filters
    - [ ] Return list of scored results with payload metadata
  - [ ] Implement `get_book_context(query: str, top_k: int, filters: Dict | None) -> str`
    - [ ] Call `search_similar()`
    - [ ] Format results into readable context string with source attribution
    - [ ] Include module/chapter/heading for each result
  - [ ] Score threshold filtering (exclude low-relevance results)
  - [ ] Error handling: empty results, Qdrant errors, timeout

### Task 4.2: Embedder Service

- [ ] Extend `Backend/Retrieval/src/retrieval/embedder.py`
  - [ ] Singleton pattern for Cohere client
  - [ ] Caching for repeated identical queries (optional)
  - [ ] Graceful degradation on API failure (return empty embeddings)

### Task 4.3: Config, Models, Validation

- [ ] Create `Backend/Retrieval/src/retrieval/models.py`
  - [ ] `SearchResult` dataclass: `text`, `score`, `module`, `chapter`, `heading`, `chunk_index`
  - [ ] `SearchResponse` dataclass: `results: List[SearchResult]`, `query`, `total_results`
- [ ] Create `Backend/Retrieval/src/retrieval/validation.py`
  - [ ] Validate query length (min 1 char, max 1000 chars)
  - [ ] Validate `top_k` range (1–20)
  - [ ] Validate collection name format
- [ ] Create `Backend/Retrieval/src/retrieval/latency.py`
  - [ ] Decorator/timer function for measuring search latency
  - [ ] Log p95, p99 search times

## Phase 5: Backend — Agent API (Week 5–6)

### Task 5.1: OpenRouter Adapter

- [ ] Create `Backend/Agent/adapter.py`
  - [ ] Implement `OpenRouterAdapter` class
  - [ ] Initialize `AsyncOpenAI` client with `OPENROUTER_BASE_URL` and `OPENROUTER_API_KEY`
  - [ ] Implement `create_agent(instructions: str, model: str, max_tokens: int) -> Agent`
    - [ ] Use `Agent` from `openai.agents`
    - [ ] Set `model` via `OpenAIChatCompletionsModel`
    - [ ] Set `ModelSettings(max_tokens=max_tokens)`
  - [ ] Implement `run_agent(agent: Agent, input: str, context: Any) -> RunResult`
    - [ ] Use `Runner.run()` with `OpenAIChatCompletionsModel`
  - [ ] Implement `run_agent_streamed(agent: Agent, input: str, context: Any) -> RunResultStreaming`
    - [ ] Use `Runner.run_streamed()` for SSE streaming
  - [ ] Implement `chat_completions_create(messages: List, model: str, max_tokens: int) -> str`
    - [ ] Direct `client.chat.completions.create()` call (used for guardrails)
    - [ ] Retry: 3 attempts with exponential backoff (2^attempt seconds)
  - [ ] Module-level singleton: `agent_adapter = OpenRouterAdapter()`
  - [ ] Error handling: rate limits, 4xx/5xx, empty responses, timeout
  - [ ] Log all API call durations and token usage

### Task 5.2: RAGAgent

- [ ] Create `Backend/Agent/agent.py`
  - [ ] Implement `RAGAgent` class
  - [ ] Constructor: accept `retrieval_service` (Retrieval module interface)
  - [ ] Implement `build_instructions(session_history: List[Dict], context: str) -> str`
    - [ ] System prompt with role as Physical AI textbook tutor
    - [ ] Include retrieved context with source attribution
    - [ ] Include session history (last 5 turns)
    - [ ] Instruct to answer in 80–100 words, no markdown, colon-label format
  - [ ] Implement `process_message(message: str, session_id: str) -> Dict`
    - [ ] Resolve context via retrieval (TOP_K=3)
    - [ ] Build instructions with session history
    - [ ] Call `agent_adapter.run_agent()`
    - [ ] Return `{"response": str, "sources": List[Dict], "session_id": str}`
  - [ ] Implement `process_message_streamed(message: str, session_id: str) -> AsyncGenerator`
    - [ ] Resolve context via retrieval (TOP_K=3)
    - [ ] Build instructions with session history
    - [ ] Call `agent_adapter.run_agent_streamed()`
    - [ ] Yield each delta as `{"token": str}`
    - [ ] On completion: yield `{"done": true, "session_id": str, "sources": List}`
  - [ ] Implement `check_off_topic(query: str) -> bool`
    - [ ] Use `chat_completions_create()` with classification prompt
    - [ ] `max_tokens=10` for quick yes/no classification
    - [ ] Return `True` if off-topic
  - [ ] Error handling: empty context, retrieval failure, LLM failure
  - [ ] Update session memory after message processing

### Task 5.3: FastAPI Application

- [ ] Create `Backend/Agent/main.py`
  - [ ] Initialize FastAPI app with title and description
  - [ ] Import and mount `sys.path` hack for Retrieval module
  - [ ] Endpoint: `POST /chat`
    - [ ] Input: `{"message": str, "session_id": str | None}`
    - [ ] Process via `RAGAgent.process_message()`
    - [ ] Return: `{"response": str, "sources": List, "session_id": str}`
  - [ ] Endpoint: `POST /chat/stream`
    - [ ] Input: `{"message": str, "session_id": str | None}`
    - [ ] Process via `RAGAgent.process_message_streamed()`
    - [ ] Return SSE stream: `text/event-stream`
  - [ ] Endpoint: `GET /health`
    - [ ] Return: `{"status": "healthy", "service": "agent", "timestamp": str}`
  - [ ] Endpoint: `GET /health/database`
    - [ ] Check Qdrant connection via `health_monitor.check_qdrant()`
    - [ ] Return: `{"status": str, "qdrant": str, "latency_ms": float}`
  - [ ] Endpoint: `GET /health/status`
    - [ ] Comprehensive health: service, database, LLM connectivity
  - [ ] Startup event: initialize settings, adapter, RAGAgent
  - [ ] Shutdown event: cleanup resources, close clients
  - [ ] CORS configuration for frontend origin
  - [ ] Custom exception handlers for 400, 404, 500, 503

### Task 5.4: Session Memory

- [ ] Implement in-memory session store in `Backend/Agent/main.py`
  - [ ] `Dict[str, List[Dict]]` — keyed by UUID session_id
  - [ ] Each entry: `[{"role": "user"|"assistant", "content": str}]`
  - [ ] Limit: `SESSION_MEMORY_TURNS` (default 5) most recent pairs
- [ ] Session ID management
  - [ ] Accept optional `session_id` in request
  - [ ] Generate new UUID if not provided
  - [ ] Validate UUID format
- [ ] TTL cleanup background task
  - [ ] Run every 5 minutes
  - [ ] Remove sessions idle > `SESSION_TTL` seconds (default 1800)
  - [ ] Log cleanup summary
- [ ] Thread safety for concurrent access

### Task 5.5: Off-Topic Guardrail

- [ ] Implement guardrail in `Backend/Agent/agent.py` (`check_off_topic`)
  - [ ] Classification prompt: "Is this question related to physical AI, humanoid robotics, or the textbook content? Answer only YES or NO."
  - [ ] Call via `agent_adapter.chat_completions_create()` with `max_tokens=10`
  - [ ] Parse response: strip whitespace, check for "YES"/"NO"
  - [ ] Default to on-topic on parse failure
- [ ] Integrate into `process_message` and `process_message_streamed`
  - [ ] Check guardrail before retrieval
  - [ ] If off-topic: return polite refusal without calling LLM
  - [ ] Stream path: yield `{"type": "off_topic", "content": "..."}`
- [ ] Log guardrail triggers for monitoring

### Task 5.6: Response Quality Validation

- [ ] Create `Backend/Agent/validation.py`
  - [ ] Implement `validate_length(response: str) -> bool`
    - [ ] Check response is within expected token range (80–150 words)
  - [ ] Implement `validate_no_markdown(response: str) -> bool`
    - [ ] Check absence of markdown symbols (`#`, `*`, `**`, `` ` ``, `---`)
  - [ ] Implement `validate_format(response: str) -> bool`
    - [ ] Check colon-label format: `Header: content`
  - [ ] Implement `validate_response(response: str) -> Dict`
    - [ ] Run all validators, return `{"passed": bool, "checks": List[str]}`
- [ ] Integrate validation into response pipeline
  - [ ] Log validation failures
  - [ ] Optionally re-request on severe quality failure

### Task 5.7: Middleware

- [ ] Create `Backend/Agent/middleware.py`
  - [ ] Rate limiting middleware
    - [ ] In-memory counter per IP: `Dict[str, List[float]]`
    - [ ] Window: 100 requests per minute
    - [ ] Return `429 Too Many Requests` with `Retry-After` header
  - [ ] Request logging middleware
    - [ ] Log method, path, status, duration, IP
    - [ ] Attach `X-Request-ID` header for tracing
  - [ ] Error logging middleware
    - [ ] Catch unhandled exceptions, log stack trace
    - [ ] Return `500` with generic error message
- [ ] Add middleware to FastAPI app in `main.py`

### Task 5.8: Health Monitoring

- [ ] Create `Backend/Agent/health_monitor.py`
  - [ ] Implement `check_qdrant() -> Dict`
    - [ ] Ping Qdrant, measure latency
    - [ ] Return `{"status": "healthy"|"unhealthy", "latency_ms": float}`
  - [ ] Implement `check_llm() -> Dict`
    - [ ] Simple `chat_completions_create()` with "ping"
    - [ ] Return `{"status": "healthy"|"unhealthy", "latency_ms": float}`
  - [ ] Cache health check results for 30 seconds (avoid hammering)

## Phase 6: Backend — Connection API (Week 7)

### Task 6.1: FastAPI Application

- [ ] Create `Backend/Connection/api.py`
  - [ ] Initialize FastAPI app with title "Physical AI Textbook — Connection API"
  - [ ] Endpoint: `POST /chat`
    - [ ] Input: `{"message": str, "session_id": str | None, "selected_text": str | None}`
    - [ ] Validate input, run guardrail, resolve context, call agent wrapper
    - [ ] Return: `{"response": str, "sources": List, "session_id": str}`
  - [ ] Endpoint: `POST /chat/stream`
    - [ ] Input: `{"message": str, "session_id": str | None, "selected_text": str | None}`
    - [ ] Return SSE stream with token deltas and completion event
  - [ ] Endpoint: `GET /health`
    - [ ] Return: `{"status": "healthy", "service": "connection", "timestamp": str}`
  - [ ] Startup event: initialize config, models, agent wrapper, sessions dict
  - [ ] Shutdown event: close HTTP client sessions
  - [ ] CORS middleware for frontend origin
  - [ ] Custom exception handlers

### Task 6.2: Context Switcher

- [ ] Create `Backend/Connection/context_switcher.py`
  - [ ] Implement `resolve_context(message: str, selected_text: str | None, top_k: int) -> Tuple[List[Dict], str]`
    - [ ] If `selected_text` is provided and non-empty: use it directly as context
    - [ ] If `selected_text` is None/empty: call Qdrant retrieval via sys.path import
    - [ ] Return `(sources_list, context_string)`
  - [ ] Sys.path hack: inject `Retrieval/src/` path before import
    - [ ] Import `from retrieval.search import get_book_context, search_similar`
  - [ ] Format context string with source metadata

### Task 6.3: Agent Wrapper

- [ ] Create `Backend/Connection/agent_wrapper.py`
  - [ ] Implement `AgentWrapper` class
  - [ ] Initialize `OpenAIChatCompletionsModel` with OpenRouter config
  - [ ] Implement `_call_agent_with_retry(agent, input, context) -> str`
    - [ ] 3 retry attempts with exponential backoff
    - [ ] Log retries
  - [ ] Implement `build_system_instructions(context: str, history: List[Dict]) -> str`
    - [ ] Role: Physical AI textbook tutor
    - [ ] Include resolved context
    - [ ] Session history (last 5 turns)
    - [ ] Output format: plain text, 80–100 words, colon labels, no markdown
  - [ ] Implement `run(message: str, context: str, history: List[Dict]) -> Dict`
    - [ ] Build instructions, create agent, call with retry
    - [ ] Return `{"response": str, "sources": List}`
  - [ ] Implement `run_streamed(message: str, context: str, history: List[Dict]) -> AsyncGenerator`
    - [ ] Build instructions, create agent, run with `run_streamed()`
    - [ ] Yield token deltas, finalize with sources
  - [ ] Guardrail integration via `GuardrailCtx`
    - [ ] `GuardrailCtx` class with `message` and `is_off_topic` flag
    - [ ] Pass via `RunContext` for `@input_guardrail` access
    - [ ] System instructions include guardrail behavior rules

### Task 6.4: Response Formatter with Source Attribution

- [ ] Create `Backend/Connection/response_formatter.py`
  - [ ] Implement `format_response(response: str, sources: List[Dict]) -> Dict`
    - [ ] Extract source metadata from retrieval results
    - [ ] Deduplicate sources by chunk
    - [ ] Include source text preview (first 100 chars)
    - [ ] Format: `{"response": str, "sources": [{"module": str, "chapter": str, "heading": str, "preview": str}]}`
  - [ ] Implement `format_error(message: str, code: str) -> Dict`
    - [ ] Standardized error response format

### Task 6.5: Session Management with TTL Cleanup

- [ ] Implement in-memory session store in `Backend/Connection/api.py`
  - [ ] `Dict[str, Dict]` with keys: `session_id`, `history: List[Dict]`, `last_active: float`
  - [ ] Max history: `SESSION_MEMORY_TURNS` (default 5)
- [ ] Session ID generation
  - [ ] `uuid.uuid4()` for new sessions
  - [ ] Accept and validate existing `session_id` from request
- [ ] TTL cleanup background task
  - [ ] Run with `asyncio.create_task` or `BackgroundTasks`
  - [ ] Remove sessions with `time.time() - last_active > SESSION_TTL`
  - [ ] Log cleanup: count removed, count remaining

### Task 6.6: Rate Limiting

- [ ] Implement in-memory rate limiter in `Backend/Connection/api.py`
  - [ ] `Dict[str, List[float]]` — IP to list of request timestamps
  - [ ] Configurable: 100 requests per minute per IP
  - [ ] Cleanup old timestamps on each request
- [ ] Integrate as dependency or middleware
  - [ ] Extract client IP from `request.client.host` or `X-Forwarded-For`
  - [ ] Return `429 Too Many Requests` with JSON body
  - [ ] Include `Retry-After` header (seconds until reset)

### Task 6.7: Input Validation and Security Patterns

- [ ] Input validation for `/chat` and `/chat/stream` endpoints
  - [ ] `message`: required, non-empty, max 2000 chars
  - [ ] `session_id`: optional, must be valid UUID if provided
  - [ ] `selected_text`: optional, max 5000 chars
- [ ] Security pattern filtering
  - [ ] Reject messages containing common prompt injection patterns
  - [ ] Strip/escape control characters
  - [ ] Length limits applied before processing
- [ ] Response sanitization
  - [ ] Strip any LLM-injected markdown
  - [ ] Ensure no SSI/server-side template injection patterns

## Phase 7: Frontend — ChatKit UI (Week 8)

### Task 7.1: Chat Component with Message Rendering

- [ ] Create `Frontend/src/components/ChatKit/ChatKit.tsx`
  - [ ] Chat container component with scrollable message list
  - [ ] Message bubble rendering (user vs assistant styling)
  - [ ] Text input field with submit button
  - [ ] Enter-to-submit, shift-enter for newline
  - [ ] Loading indicator during request
  - [ ] Error state display (connection errors, off-topic messages)
  - [ ] Source attribution display (expandable source footnotes)
  - [ ] Auto-scroll to bottom on new messages
  - [ ] Empty state prompt
- [ ] Create `Frontend/src/components/ChatKit/ChatKit.css`
  - [ ] Styled chat container matching glassmorphism theme
  - [ ] User message: right-aligned, cyan accent
  - [ ] Assistant message: left-aligned, glass background
  - [ ] Source footer: subtle, smaller text
  - [ ] Input area with glass effect

### Task 7.2: SSE Streaming via ReadableStream

- [ ] Implement SSE consumption in `ChatKit.tsx`
  - [ ] Use `fetch()` to POST to `/chat/stream`
  - [ ] Read response body with `ReadableStream` via `response.body.getReader()`
  - [ ] Decode `TextDecoder` stream chunks
  - [ ] Parse SSE lines: `data: {...}\n\n`
  - [ ] Handle `token` events: append to current assistant message incrementally
  - [ ] Handle `done` event: finalize message, update session_id
  - [ ] Handle `off_topic` event: render off-topic warning card
  - [ ] Handle `error` event: display error message
  - [ ] Abort controller cleanup on unmount
  - [ ] Timeout handling (30s idle stream timeout)
- [ ] Fallback to POST `/chat` if streaming fails
  - [ ] Detect SSE connection failure
  - [ ] Fall back to synchronous POST request
  - [ ] Show non-streaming response as complete message

### Task 7.3: Session ID Tracking and POST Fallback

- [ ] Session ID lifecycle in `ChatKit.tsx`
  - [ ] Initialize `session_id` from `useState` (null initially)
  - [ ] Update `session_id` from stream `done` event or POST response
  - [ ] Persist `session_id` in localStorage per session
  - [ ] Include `session_id` in every request
- [ ] POST fallback implementation
  - [ ] On stream failure, re-send as `POST /chat`
  - [ ] Handle synchronous response rendering
  - [ ] Log fallback events for monitoring

### Task 7.4: Multi-Session localStorage Persistence with 48h Auto-Delete

- [ ] Implement localStorage persistence layer
  - [ ] Key format: `chat_sessions` — JSON array of session metadata
  - [ ] Key format: `chat_session_{id}` — full message history per session
  - [ ] Save on each new message (debounced)
- [ ] Session metadata structure
  - [ ] `session_id`, `title` (first user message), `created_at`, `updated_at`, `message_count`
- [ ] Auto-delete stale sessions
  - [ ] On app load, filter sessions with `updated_at < now - 48h`
  - [ ] Remove stale session data from localStorage
  - [ ] Log cleanup count
- [ ] Session switching
  - [ ] Load selected session from localStorage
  - [ ] Cache current session in memory
- [ ] SSR safety
  - [ ] All `localStorage` calls wrapped in `try/catch`
  - [ ] Skip persistence if `localStorage` unavailable

### Task 7.5: History Panel

- [ ] Create history panel component
  - [ ] Slide-out drawer or sidebar section
  - [ ] Session list with title, date, message count preview
  - [ ] Click to switch sessions
  - [ ] Delete button per session (with confirmation)
  - [ ] New chat button to create fresh session
  - [ ] Refresh button to reload from localStorage
- [ ] Session preview
  - [ ] Show first 2 messages as mini preview
  - [ ] Show relative time (2h ago, yesterday, etc.)
- [ ] Empty state when no sessions exist

### Task 7.6: Off-Topic Error Rendering

- [ ] Off-topic message card component
  - [ ] Distinct styling (amber/warning color)
  - [ ] Icon indicator
  - [ ] Message: "I specialize in Physical AI and humanoid robotics topics. Please ask me something related to the textbook content."
  - [ ] Dismiss button
- [ ] Handle off-topic in stream flow
  - [ ] Render warning card when `{"type": "off_topic"}` received
  - [ ] Prevent further token accumulation

### Task 7.7: SSR-Safe try/catch for Browser APIs

- [ ] Audit all browser API usage
  - [ ] `localStorage.getItem`, `setItem`, `removeItem` in `try/catch`
  - [ ] `window` access in `try/catch`
  - [ ] `document` access in `try/catch`
  - [ ] `navigator` access in `try/catch`
- [ ] No browser API calls in module scope
  - [ ] All calls inside `useEffect`, event handlers, or lazy initializers
- [ ] Fallback values when API unavailable
  - [ ] `localStorage` unavailable → in-memory only
  - [ ] `window` unavailable → no-op

## Phase 8: Frontend — UI Theme (Week 9)

### Task 8.1: Glassmorphism Dark Theme

- [ ] Create `Frontend/src/css/custom.css`
  - [ ] Define CSS custom properties
    - [ ] `--ifm-color-primary`: Electric Cyan `#06B6D4`
    - [ ] `--ifm-background-color`: Midnight Blue `#0A0E1A`
    - [ ] `--glass-bg`: `rgba(255, 255, 255, 0.05)`
    - [ ] `--glass-border`: `rgba(255, 255, 255, 0.1)`
    - [ ] `--glass-blur`: `blur(12px)`
  - [ ] Apply glassmorphism to `.navbar`, `.footer`, `.card`, `.docSidebarContainer`
  - [ ] Apply dark theme scrollbar styling
  - [ ] Typography: system font stack, comfortable line-height
  - [ ] Link styling: cyan underline on hover
  - [ ] Code block styling: dark background with cyan accents
  - [ ] Table styling: glass borders, alternating row backgrounds
  - [ ] Responsive breakpoints for mobile

### Task 8.2: Hero Page with Mesh Gradients

- [ ] Create `Frontend/src/pages/index.module.css`
  - [ ] Hero section full-screen layout
  - [ ] Mesh gradient background (`radial-gradient` overlays)
  - [ ] Title text with gradient fill
  - [ ] Subtitle with subtle glow
  - [ ] CTA buttons with glass effect
  - [ ] Animated particles or subtle movement (CSS keyframes)
  - [ ] Module card grid (4 cards linking to each module)
  - [ ] Responsive: stack cards on mobile

### Task 8.3: Sidebar, TOC, Footer Styles

- [ ] Sidebar styling (`Frontend/src/theme/DocSidebar/styles.module.css`)
  - [ ] Glass background with scrollbar styling
  - [ ] Category headers with cyan left border
  - [ ] Active link highlighting
  - [ ] Hover effects on sidebar items
- [ ] TOC styling (`Frontend/src/theme/TOC/styles.module.css`)
  - [ ] Sticky positioning
  - [ ] Glass effect with thin border
  - [ ] Active heading highlight
  - [ ] Compact layout
- [ ] Footer styling (`Frontend/src/theme/Footer/styles.module.css`)
  - [ ] Glass background at page bottom
  - [ ] Links in cyan, subtle hover
  - [ ] Social/media icons row
  - [ ] Copyright text in muted color
- [ ] Navbar styling (`Frontend/src/theme/Navbar/styles.module.css`)
  - [ ] Glass background with backdrop blur
  - [ ] Sticky top with shadow on scroll
  - [ ] Search bar styling
  - [ ] Dark mode toggle styling

## Phase 9: Orchestration & Deployment (Week 10)

### Task 9.1: Orchestrator

- [ ] Create `Backend/main.py`
  - [ ] Orchestrator entry point for local development
  - [ ] Spawn 3 processes:
    1. Retrieval test process (quick health check)
    2. Agent API (`uvicorn Agent.main:app --port 8001`)
    3. Connection API (`uvicorn Connection.api:app --port 8000`)
  - [ ] Use `multiprocessing` module
  - [ ] Graceful shutdown on `SIGINT`/`SIGTERM`
  - [ ] Log process start/stop events
  - [ ] Restart crashed processes (optional health monitor)

### Task 9.2: Hugging Face Spaces Entry Point

- [ ] Create `Backend/app.py`
  - [ ] HF Spaces entry point (port 7860)
  - [ ] Start Connection API only (`uvicorn Connection.api:app --host 0.0.0.0 --port 7860`)
  - [ ] Load `.env` from repo root
  - [ ] Set `DEBUG=False` in production
- [ ] Configure Spaces secrets
  - [ ] `OPENROUTER_API_KEY`, `COHERE_API_KEY`, `QDRANT_API_KEY`, `QDRANT_URL`

### Task 9.3: Dockerfile

- [ ] Create `Backend/Dockerfile`
  - [ ] Base image: `python:3.13-slim`
  - [ ] Install `uv` via pip
  - [ ] Set working directory to `/app`
  - [ ] Copy `Backend/` contents
  - [ ] Run `uv sync` to install dependencies
  - [ ] Expose port 7860
  - [ ] Entry point: `CMD ["python", "app.py"]`
  - [ ] Health check: `HEALTHCHECK --interval=30s --timeout=10s CMD curl -f http://localhost:7860/health || exit 1`

### Task 9.4: Vercel Config

- [ ] Create `vercel.json` at repo root
  - [ ] Build command: `cd Frontend && npm install && npm run build`
  - [ ] Output directory: `Frontend/build`
  - [ ] Framework: `docusaurus`
  - [ ] Rewrites/redirects for SPA fallback
- [ ] Environment variables on Vercel
  - [ ] `BACKEND_URL` — production HF Spaces URL

## Phase 10: Testing (Week 11)

### Task 10.1: Agent Unit Tests

- [ ] Create `Backend/Agent/tests/test_adapter.py`
  - [ ] Test `OpenRouterAdapter` initialization
  - [ ] Test `create_agent` returns `Agent` instance
  - [ ] Test `chat_completions_create` with mock responses
  - [ ] Test retry logic on failure (3 attempts)
  - [ ] Test exponential backoff timing
  - [ ] Test singleton pattern
  - [ ] Mock all external API calls
- [ ] Create `Backend/Agent/tests/test_agent.py`
  - [ ] Test `RAGAgent` initialization
  - [ ] Test `build_instructions` includes context and history
  - [ ] Test `process_message` returns correct format
  - [ ] Test `process_message_streamed` yields token deltas
  - [ ] Test `check_off_topic` returns boolean
  - [ ] Test off-topic trigger prevents LLM call
  - [ ] Test empty context handling
  - [ ] Mock adapter and retrieval
- [ ] Create `Backend/Agent/tests/test_core_functionality.py`
  - [ ] Integration test: end-to-end flow with mocked dependencies
  - [ ] Test session memory limits (5 turns)
  - [ ] Test session TTL cleanup
  - [ ] Test concurrent session isolation
  - [ ] Test error handling for all failure modes

### Task 10.2: Ingestion Tests

- [ ] Create `Backend/Ingestion/tests/test_chunker.py`
  - [ ] Test markdown-aware splitting by headings
  - [ ] Test chunk size and overlap constraints
  - [ ] Test metadata preservation (heading hierarchy)
  - [ ] Test edge cases: empty content, no headings, code blocks
- [ ] Create `Backend/Ingestion/tests/test_vectorizer.py`
  - [ ] Test DocumentChunk to Qdrant point conversion
  - [ ] Test UUID generation (deterministic per chunk)
  - [ ] Test empty chunk filtering
- [ ] Create `Backend/Ingestion/tests/test_document_processor.py`
  - [ ] Test markdown file parsing
  - [ ] Test frontmatter extraction
  - [ ] Test section splitting
  - [ ] Test error handling for malformed files
- [ ] Create `Backend/Ingestion/tests/test_text_cleaner.py`
  - [ ] Test markdown removal
  - [ ] Test whitespace normalization
  - [ ] Test special character handling
- [ ] Create `Backend/Ingestion/tests/test_embedder.py`
  - [ ] Test Cohere client initialization
  - [ ] Test embedding with mock response
- [ ] Create `Backend/Ingestion/tests/test_vector_store.py`
  - [ ] Test upsert with mocked Qdrant
  - [ ] Test collection delete

### Task 10.3: Connection API Integration Tests

- [ ] Create `Backend/Connection/tests/`
  - [ ] Test `POST /chat` with valid input
  - [ ] Test `POST /chat/stream` SSE output format
  - [ ] Test `GET /health` response
  - [ ] Test `context_switcher` with `selected_text` vs retrieval
  - [ ] Test rate limiter returns 429 after threshold
  - [ ] Test session persistence across requests
  - [ ] Test off-topic guardrail returns refusal
  - [ ] Test input validation rejects empty/long messages
  - [ ] Test security pattern filters
  - [ ] Test CORS headers
  - [ ] Test error responses for all endpoints
  - [ ] Mock Agent API and Retrieval for isolated testing
